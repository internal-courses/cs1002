# Replay Exemplars For “How Students Learn Python”

_Generated by `analysis/generate_replays_md.py` on 2026-03-13 16:35 UTC._

This memo is optimized for talk preparation. It prioritizes questions where the same prompt produces visibly different approaches to problem solving, debugging, and failure.

How to use it:

- Open with the shortest successful replay for a question so the audience sees the clean mental model first.
- Then show one productive-struggle replay and one unstable or incomplete replay on the same question.
- Treat the examples as question-local comparisons. Do not use them for naive cross-term claims; later terms are progression-filtered by course design.

Selection notes after review:

- `Shuffle a Three Word Sentence` is now in the main set because it is the clearest sample-overfitting/public-pass/private-fail family in the repo.
- Several opener cases were replaced with genuinely shortest correct replays so each question starts with a clean baseline.
- The funny appendix is now broader than the core pedagogical set, because the funniest stage moments are not always the best full teaching questions.
- A separate funny-only question order now appears at the end so the talk can front-load laughs without losing the stronger analytical exemplars.

Replay generation note:

- For any tuple below, generate a fresh cast with `uv run --script analysis/generate_asciinema.py --namespace ... --problem-id ... --student-id ... --output analysis/replay-...rec`.

## Card to Value Tuple (`ns_25t2_py11_1/6`)

A compact parsing-and-mapping question whose hidden edge case is unusually teachable: many students write a perfectly plausible two-character parser that passes public tests and then fails on multi-character ranks like `10D`.

- Track A submitters: `276`
- Full-pass rate among submitters: `17.8%`
- Average public test runs: `10.9`
- Dominant archetypes: Steady builder (49), Minimal-change solver (46), Skeleton-only (43), Volatile reworker (39)
- Source artifacts: [problem JSON](../problems/ns_25t2_py11_1/6.json), [same-code gap table](./classical_item_quality/public_private_gap_same_code_by_question.csv)

What this question reveals:

- This is the strongest same-code public-pass/private-fail question in the repo, so it is the cleanest exhibit for hidden-test overfitting.
- It reveals a precise novice assumption: `card[0]` is always the rank and `card[1]` is always the suit.
- The best replays are very short, which makes this a strong opener for a talk section on question design.

### Fast exact parser

- Student ID: `e42a471813ae41a7b30d2f6927d92c32`
- Primary archetype: `Minimal-change solver`
- Outcome: `Full pass`
- Final score: `100`
- Event count: `4`
- Public test runs: `2`
- Active time: `0:10`
- Replay tuple: `ns_25t2_py11_1 / 6 / e42a471813ae41a7b30d2f6927d92c32`

What the student actually does:

- Uses `card[:-1]` for rank, `card[-1]` for suit, and dictionary lookups from the first snapshot.

Why it is interesting:

- A true baseline replay: the student models variable-width input correctly before touching the keyboard much.

Key moments in the replay:

- `0s`: first public run already passes `2/2`.
- `9s`: the same code immediately passes private `2/2`.
- `9s`: submission happens with no substantive edits after the first working version.

What it illustrates about student learning:

- Model parsing by input shape, not by the width of the visible examples.
- This replay is a good reminder that strong students often solve format questions by decomposing the contract before coding.

How it can inform question design:

- Questions with variable-width inputs reward students who separate rank and suit semantically instead of by hard-coded positions.
- This is a strong contrast case for the much weaker one-character parsers below.

### Steady builder / contract repair

- Student ID: `56036b0cfd0a453da93c959861c50f96`
- Primary archetype: `Steady builder`
- Outcome: `Full pass`
- Final score: `100`
- Event count: `6`
- Public test runs: `4`
- Active time: `0:40`
- Replay tuple: `ns_25t2_py11_1 / 6 / 56036b0cfd0a453da93c959861c50f96`

What the student actually does:

- Parses rank and suit correctly from the start with an explicit `if/elif` ladder, but first trips on tuple construction and then on the required output order.

Why it is interesting:

- An excellent compact contract-debugging replay: the hard part is not card logic, it is returning the exact structure `(suit_value, rank_value)`.

Key moments in the replay:

- `0s`: runtime error from `return tuple(a,b)`.
- `14s`: first runnable version returns `(a,b)` and still fails because the tuple order is reversed.
- `32s`: swapping to `(b,a)` reaches public `2/2`.
- `39s`: private `100`; submission follows on the same fixed code.

What it illustrates about student learning:

- Separate 'how do I compute the pieces?' from 'what exact structure must I return?' when teaching novice debugging.
- Students often need explicit prompting to treat tuple order as part of the specification, not decoration.

How it can inform question design:

- Public tests do useful work here because they catch tuple/type/order mistakes before the hidden edge case even appears.
- This is a well-designed short question for teaching representation and contract checking without a large algorithmic load.

### Public-pass / hidden-case overfitter

- Student ID: `ad7a05553f034a4a9766e6061a80ed7f`
- Primary archetype: `Minimal-change solver`
- Outcome: `Partial pass`
- Final score: `50`
- Event count: `5`
- Public test runs: `3`
- Active time: `1:02`
- Replay tuple: `ns_25t2_py11_1 / 6 / ad7a05553f034a4a9766e6061a80ed7f`

What the student actually does:

- Repairs early indexing mistakes, lands on a neat two-character parser with `card[1]` and `card[0]`, and submits after public success even though multi-character ranks are still impossible.

Why it is interesting:

- This is the repo's clearest 'visible tests are green, the real spec is not' replay in miniature.

Key moments in the replay:

- `0s`: runtime error from indexing as if the string had nested characters (`card[0][1]`).
- `56s`: public `2/2` with a one-character parser using `card[1]` and `card[0]`.
- `61s`: private drops to `50`; the hidden case exposes that `'10D'` is not a two-character card.
- `62s`: submission still happens at `50`, freezing the overfit in place.

What it illustrates about student learning:

- Teach students to ask 'what family of unseen inputs would break this parser?' before trusting green public tests.
- This replay is ideal for showing how overfitting can look rational from the student's point of view.

How it can inform question design:

- If public tests only show one-character ranks, hidden tests must include `10`; this question proves why.
- For instruction, consider making `10D` a visible counterexample or explicitly asking students to propose one hidden case of their own.

## Check For Greeting Prefix (`ns_25t2_py22_1/15`)

A deceptively simple specification question that exposes whether students read semantics precisely: case sensitivity, required trailing space, leading-space negatives, and empty-string safety.

- Track A submitters: `982`
- Full-pass rate among submitters: `58.8%`
- Average public test runs: `12.8`
- Dominant archetypes: Volatile reworker (247), Minimal-change solver (219), Steady builder (188), Incremental debugger (123)
- Source artifacts: [problem JSON](../problems/ns_25t2_py22_1/15.json), [question error analysis](./ERRORS-cluster-c078-check-for-greeting-prefix-969f783c.md)

What this question reveals:

- The problem looks trivial, which makes it excellent for showing how often failure is really spec-reading failure.
- It creates strong public-versus-private moments because many students solve a narrower rule than the real prompt.
- The replay contrast is especially good for teaching boundary-case reasoning.

### Shortest semantics-first solver

- Student ID: `0fdf6645bdc54e7da88566e0422fbda1`
- Primary archetype: `Minimal-change solver`
- Outcome: `Full pass`
- Final score: `100`
- Event count: `5`
- Public test runs: `1`
- Active time: `0:01`
- Replay tuple: `ns_25t2_py22_1 / 15 / 0fdf6645bdc54e7da88566e0422fbda1`

What the student actually does:

- The first observed code is already the whole solution: `s.startswith('Hello ') or s.startswith('Hi ')`.

Why it is interesting:

- A better opening exemplar than the previous longer baseline. There is almost nothing to narrate except correct semantic translation of the prompt.

Key moments in the replay:

- Event 1: the first observed checkpoint already passes private `3/3`.
- Event 2: the same code also passes public `4/4`.
- Event 3: submission follows immediately with unchanged code.

What it illustrates about student learning:

- Use this replay to model translating a natural-language rule into a tiny set of exact string predicates.
- It is the ideal opening contrast before showing spec-reading failures and public-test overfitting.

How it can inform question design:

- The question is valuable because it discriminates between vague prefix intuition and exact contract reading.
- Its hidden tests still matter even though this student did not need them; they keep weaker prefix heuristics from looking equivalent.

### Compact hidden-test repair

- Student ID: `aa68a2811ed74d968987be81d3d6fb31`
- Primary archetype: `Steady builder`
- Outcome: `Full pass`
- Final score: `100`
- Event count: `9`
- Public test runs: `4`
- Active time: `0:34`
- Replay tuple: `ns_25t2_py22_1 / 15 / aa68a2811ed74d968987be81d3d6fb31`

What the student actually does:

- Starts with the almost-right rule `startswith('Hello') or startswith('Hi')`, then corrects it to require the trailing space after a single hidden-test miss.

Why it is interesting:

- A much tighter replay for the same boundary-semantics lesson. You can see the exact missing condition appear on screen in one edit.

Key moments in the replay:

- Event 1: public `3/4`; missing-space prefixes look correct on the visible tests.
- Event 2: private `67`; hidden tests reject `startswith('Hello')` / `startswith('Hi')` without the trailing space.
- Event 4: public `4/4` after adding the spaces.
- Event 5: private `100`; event 7: final submission.

What it illustrates about student learning:

- This is an ideal clip for asking, 'What exact contract did the public tests fail to force you to notice?'
- It shows students that one hidden-case miss often means one missing condition, not a need for a wholesale rewrite.

How it can inform question design:

- The question is well tuned when a one-condition repair can convert a nearly-right solution into a correct one.
- Keeping the public tests slightly under-specified here is pedagogically useful because the hidden failure still points to a coherent missing boundary.

### Regression on edge cases

- Student ID: `590240758edf48fa81f701ae4295dc82`
- Primary archetype: `Regression`
- Outcome: `Partial pass`
- Final score: `66.7`
- Event count: `131`
- Public test runs: `72`
- Active time: `1:36:49`
- Replay tuple: `ns_25t2_py22_1 / 15 / 590240758edf48fa81f701ae4295dc82`

What the student actually does:

- Begins with a broad `strip()` plus `startswith('Hello')/'Hi'` rule, briefly constructs a better boundary-aware version, then reverts to the weaker logic and leaves the improved code commented out.

Why it is interesting:

- A near-solver who appears to understand the task but never fully locks down the edge cases. This case helps the audience see how private-test misses often come from one semantic gap, not total confusion.

Key moments in the replay:

- `0s`, event 1: runtime error.
- `684s`, event 7: first private `67` with `s.strip(); startswith('Hello'/'Hi')` logic.
- `4772s`, event 16: public reaches `4/4` with a more careful boundary-aware approach.
- `5809s`, event 131: final submission falls back to `67`; the better logic is left inside a triple-quoted comment block.

What it illustrates about student learning:

- Students need help naming the exact counterexamples they are trying to eliminate: empty string, no trailing space, leading whitespace.
- This is a good replay for teaching checkpoint-based debugging after partial private success.

How it can inform question design:

- The question's most valuable design feature is that its edge cases are conceptually coherent rather than arbitrary.
- Keep the public tests sparse enough to create a private-test reveal, but not so sparse that students cannot orient themselves.

## Shuffle a Three Word Sentence (`ns_25t3_py13_1/7`)

A compact permutation problem that is unusually good for showing sample overfitting, finite-state brute forcing, and the difference between using `order` as data versus memorizing visible cases.

- Track A submitters: `518`
- Full-pass rate among submitters: `59.1%`
- Average public test runs: `14.3`
- Dominant archetypes: Steady builder (192), Minimal-change solver (94), Volatile reworker (84), Builder with setbacks (28)
- Source artifacts: [problem JSON](../problems/ns_25t3_py13_1/7.json), [question error analysis](./ERRORS-cluster-c002-shuffle-a-three-word-sentence-6b942fc6.md)

What this question reveals:

- This is the clearest missing pattern from the earlier memo: students sometimes solve the public examples rather than the task.
- The question is especially strong for discussing public-test design because one hard-coded sentence can pass all visible checks.
- It also reveals that some students prefer exhaustive case enumeration over the intended tuple-driven abstraction.

### Shortest generic solver

- Student ID: `ebd2cfa0ce7e4554850c3bc999fa10e2`
- Primary archetype: `Minimal-change solver`
- Outcome: `Full pass`
- Final score: `100`
- Event count: `3`
- Public test runs: `1`
- Active time: `0:01`
- Replay tuple: `ns_25t3_py13_1 / 7 / ebd2cfa0ce7e4554850c3bc999fa10e2`

What the student actually does:

- Uses the intended abstraction from the start: split the sentence into words, reorder them by `order`, and join them back with spaces.

Why it is interesting:

- The clean baseline for this family. It makes the later hard-coded and case-by-case solutions look obviously like different ways of thinking.

Key moments in the replay:

- Event 1: the first observed checkpoint already passes private `3/3`.
- Event 2: the same code also passes public `3/3`.
- Event 3: submission follows immediately with unchanged code.

What it illustrates about student learning:

- Use this replay to show what it looks like when a student treats `order` as data instead of as a menu of special cases.
- It is a good opening clip because the intended abstraction is visible in one line.

How it can inform question design:

- The task has a very readable direct solution, which makes wrong abstractions easy to contrast on stage.
- It is a good talk question because the correct code is short enough to hold in working memory.

### Public-sample hardcoder

- Student ID: `384851c6834647139983873aea99d419`
- Primary archetype: `Steady builder`
- Outcome: `Submitted, zero`
- Final score: `0`
- Event count: `6`
- Public test runs: `4`
- Active time: `2:10`
- Replay tuple: `ns_25t3_py13_1 / 7 / 384851c6834647139983873aea99d419`

What the student actually does:

- Writes a branch like `if order==(0, 2, 1): return 'apple orange banana'`, effectively memorizing one public example instead of using the input sentence generically.

Why it is interesting:

- One of the cleanest public-pass/private-fail replays in the repo. The student literally learns a visible example and nothing else.

Key moments in the replay:

- Event 1: public `1/3` with a single hard-coded example output.
- Event 2: public `2/3`; event 3: public `3/3`, all with the same sample-specific idea.
- Event 4: private immediately drops to `0/3`.
- Event 6: final submission stays at `0`.

What it illustrates about student learning:

- This is the replay to use when explaining overfitting in the most concrete possible way.
- It helps instructors name the behavior kindly but precisely: the student solved the examples, not the rule.

How it can inform question design:

- This question demonstrates why public tests need at least one anti-hardcoding sentinel with unseen words.
- If the public set teaches only three visible permutations, some students will infer that memorizing those is the task.

### Finite-state brute forcer

- Student ID: `107337b2583a4bfebe3e917b315d2684`
- Primary archetype: `Builder with setbacks`
- Outcome: `Full pass`
- Final score: `100`
- Event count: `9`
- Public test runs: `7`
- Active time: `8:27`
- Replay tuple: `ns_25t3_py13_1 / 7 / 107337b2583a4bfebe3e917b315d2684`

What the student actually does:

- Enumerates all six possible three-word permutations with explicit `if/elif` branches rather than using the tuple as an indexing plan.

Why it is interesting:

- A subtle but important contrast: the code is correct, yet it avoids the intended abstraction. It shows that students can reach correctness by exhaustively covering a tiny state space.

Key moments in the replay:

- Events 1-4: public remains below full pass while the student hand-builds case branches and still has spacing mistakes.
- Event 6: public reaches `3/3` once the branch outputs are formatted correctly.
- Event 7: private also reaches `100`; event 9: submission.

What it illustrates about student learning:

- Use this replay to discuss the difference between 'correct for this tiny universe' and 'using a transferable abstraction.'
- It is a strong reminder that some students reason by exhaustive cases before they reason by data structure.

How it can inform question design:

- If the instructional goal is tuple-driven indexing, a three-word universe is small enough that brute force can also win.
- That is not automatically bad, but it means the item measures correctness more than abstraction choice.

## Pangram Check (`ns_25t2_py21_2/18`)

A classic 'looks easy, is actually conceptual' problem about coverage, filtering, and control flow. It reliably reveals early returns, sample overfitting, and confusion between total characters and distinct letters.

- Track A submitters: `675`
- Full-pass rate among submitters: `51.4%`
- Average public test runs: `15.6`
- Dominant archetypes: Volatile reworker (173), Minimal-change solver (157), Steady builder (77), Incremental debugger (64)
- Source artifacts: [problem JSON](../problems/ns_25t2_py21_2/18.json), [question error analysis](./ERRORS-cluster-c013-pangram-check-f0d5ae7d.md)

What this question reveals:

- This is one of the clearest repo examples of public-pass illusion followed by private-test correction.
- The problem cleanly separates checking for any alphabetic text from checking coverage of all 26 letters.
- It is ideal for discussing hidden tests as a prompt to revisit the rule, not to guess more code.

### Shortest clean invariant

- Student ID: `13bc0b2cf15145219dd6719b89dfc3cd`
- Primary archetype: `Minimal-change solver`
- Outcome: `Full pass`
- Final score: `100`
- Event count: `3`
- Public test runs: `1`
- Active time: `0:00`
- Replay tuple: `ns_25t2_py21_2 / 18 / 13bc0b2cf15145219dd6719b89dfc3cd`

What the student actually does:

- Represents the task directly from the start: build the alphabet as a set and check whether it is a subset of `set(text.lower())`.

Why it is interesting:

- A stronger opening baseline than the previous pick. It shows the exact invariant with almost no replay overhead.

Key moments in the replay:

- Event 1: the first observed checkpoint already passes private `3/3`.
- Event 2: the same code also passes public `3/3`.
- Event 3: submission follows immediately with unchanged code.

What it illustrates about student learning:

- Use this replay to establish the target representation before showing heuristic shortcuts and false summits.
- It makes later failures easier to interpret because the audience has already seen the right invariant cleanly.

How it can inform question design:

- This item is strong because the core solution is simple and teachable once students choose the right representation.
- It rewards conceptual clarity more than syntax juggling.

### Public-pass then private-fix solver

- Student ID: `2ee6740d56614ebbb3e68f6fe2992f28`
- Primary archetype: `Volatile reworker`
- Outcome: `Full pass`
- Final score: `100`
- Event count: `107`
- Public test runs: `72`
- Active time: `1:51:23`
- Replay tuple: `ns_25t2_py21_2 / 18 / 2ee6740d56614ebbb3e68f6fe2992f28`
- Existing replay artifacts: [narrative JSON](./replay-ns_25t2_py21_2-18-2ee6740d56614ebbb3e68f6fe2992f28.json), [asciinema replay](./replay-ns_25t2_py21_2-18-2ee6740d56614ebbb3e68f6fe2992f28.rec)

What the student actually does:

- Starts with an early-return rule that rejects non-letters, then a length/count heuristic, and only late in the session adopts the real invariant: unique alphabetic letters.

Why it is interesting:

- A strong replay for the moment when public success creates false confidence, then hidden cases force a deeper repair of the core logic.

Key moments in the replay:

- `0s`, event 1: public partial; code returns `False` too early on non-letters.
- `46.9s`, event 61: public all-pass with the wrong `count >= 26` heuristic.
- `47.2s`, event 62: private fails immediately; hidden cases punish total-letter counting.
- `70.6s`, event 103: private all-pass after switching to a `set()` of unique letters.
- `71.4s`, event 107: final submission `100`.

What it illustrates about student learning:

- Show this replay when explaining why students must ask 'what family of inputs is this code really solving?'
- It is especially useful for teaching how to recover after the first private-test failure without panicking.

How it can inform question design:

- The hidden tests are excellent here because they expose exactly the shortcut strategies we want to discourage.
- This question works well in a talk because the transition from apparent success to real success is easy to narrate.

### Short false summit on filtering

- Student ID: `ce4de84afb3e4d219f3688124ec46b12`
- Primary archetype: `Minimal-change solver`
- Outcome: `Partial pass`
- Final score: `33.3`
- Event count: `5`
- Public test runs: `3`
- Active time: `0:43`
- Replay tuple: `ns_25t2_py21_2 / 18 / ce4de84afb3e4d219f3688124ec46b12`

What the student actually does:

- Gets to a plausible `set`-equality solution quickly, but filters out only spaces. That is enough for the public tests and still wrong for hidden punctuation cases.

Why it is interesting:

- Cleaner and shorter than the previous long regression pick. The audience can see the exact missing subproblem in one glance: filtering non-letters, not just spaces.

Key moments in the replay:

- Event 1: first public run only reaches `1/3` because case handling is incomplete.
- Event 2: lowercasing repair reaches public `3/3`.
- Event 3: private immediately drops to `33`; punctuation and other non-letters still poison the set.
- Event 5: final submission stays at `33`.

What it illustrates about student learning:

- This is a good replay for teaching 'filter, then reason' rather than assuming spaces are the only noise.
- It also shows how one successful public rerun can freeze thinking before the invariant is actually complete.

How it can inform question design:

- Hidden tests should include punctuation and digits, not just mixed case, to expose incomplete filtering rules.
- This question works well because the private failure points to a coherent missing case rather than an arbitrary exception.

## Find Characters Appearing More Than Once (`ns_25t3_py13_1/10`)

A very good same-question comparison for order-sensitive reasoning. Students often find duplicates, but not in the required first-appearance order, or they destroy the order with sets.

- Track A submitters: `564`
- Full-pass rate among submitters: `43.8%`
- Average public test runs: `17.8`
- Dominant archetypes: Volatile reworker (127), Steady builder (81), Minimal-change solver (76), Regression (74)
- Source artifacts: [problem JSON](../problems/ns_25t3_py13_1/10.json), [question error analysis](./ERRORS-cluster-c022-find-characters-appearing-more-than-once-a831cf60.md)

What this question reveals:

- This question exposes a subtle but teachable distinction between solving the data problem and solving the contract.
- It is a good talk example of how 'I found the repeated characters' is not the same as 'I met the output spec'.
- The contrast cases here are especially strong for discussing representation choices like `set`, `count`, and ordered scans.

### Shortest clean baseline

- Student ID: `24bf4a098ea84ff48f0461396cb53a29`
- Primary archetype: `Minimal-change solver`
- Outcome: `Full pass`
- Final score: `100`
- Event count: `3`
- Public test runs: `1`
- Active time: `0:00`
- Replay tuple: `ns_25t3_py13_1 / 10 / 24bf4a098ea84ff48f0461396cb53a29`

What the student actually does:

- Writes a direct ordered scan from the start: walk through the string, use `s.count(ch) > 1` to detect repetition, and append only the first time each repeated character is seen.

Why it is interesting:

- A cleaner opening exemplar for this question. The first observed code already satisfies the order contract, so later failures look like true representation mistakes rather than spec ambiguity.

Key moments in the replay:

- Event 1: the first observed checkpoint already passes private `3/3`.
- Event 2: the same code also passes public `3/3`.
- Event 3: submission follows immediately with unchanged code.

What it illustrates about student learning:

- Use this replay to establish the contract clearly: repeated characters, first-appearance order, no duplicates in the output list.
- It provides a clean anchor before showing wrong-unit reasoning like `split()` or order-destroying `set` logic.

How it can inform question design:

- This question is strong because the correct output contract is readable enough to compare directly against wrong abstractions.
- A simple clean solution makes later set-order and representation failures more teachable.

### Short order-repair debugger

- Student ID: `9059d198c33b4b349eb2af1315239956`
- Primary archetype: `Incremental debugger`
- Outcome: `Full pass`
- Final score: `100`
- Event count: `10`
- Public test runs: `8`
- Active time: `4:15`
- Replay tuple: `ns_25t3_py13_1 / 10 / 9059d198c33b4b349eb2af1315239956`

What the student actually does:

- Starts by using `set(s)`-style reasoning that destroys order, then briefly sorts the repeated characters, and finally switches to an ordered accumulation that preserves first appearance.

Why it is interesting:

- A much shorter version of the same point. The order bug is visible immediately, and the repair is easy to narrate in under a minute.

Key moments in the replay:

- Event 1: public `1/3`; the first attempt relies on `set(s)`, so the repeated characters lose first-appearance order.
- Events 5-6: public `2/3`; switching to `set(filter(...))` and then `sorted(...)` fixes membership but still not the contract.
- Event 7: public `3/3` once the answer is rebuilt in input order.
- Event 8: private `100`; event 10: submission.

What it illustrates about student learning:

- This is the cleanest short clip for teaching that sets answer the wrong question when order matters.
- It is useful for showing how a student can fix the abstraction without changing the task itself.

How it can inform question design:

- The item is well-designed because one good hidden case can separate true order handling from fragile `set`-based solutions.
- For teaching, pair the replay with a visual trace of `seen` and `repeated` state.

### Early win, then regression

- Student ID: `ee012cee3fa5491d8db37141d2a954fe`
- Primary archetype: `Regression`
- Outcome: `Partial pass`
- Final score: `33.3`
- Event count: `218`
- Public test runs: `133`
- Active time: `1:23:30`
- Replay tuple: `ns_25t3_py13_1 / 10 / ee012cee3fa5491d8db37141d2a954fe`
- Existing replay artifacts: [narrative JSON](./replay-ns_25t3_py13_1-10-ee012cee3fa5491d8db37141d2a954fe.json), [asciinema replay](./replay-ns_25t3_py13_1-10-ee012cee3fa5491d8db37141d2a954fe.rec)

What the student actually does:

- Spends most of the session trapped in the same bad idea family: list/set conversions and mixed return types that destroy order or the required output structure.

Why it is interesting:

- One of the strongest regression exemplars in the repo: early public success, immediate private failure, then repeated destabilizing edits that leave the student worse off.

Key moments in the replay:

- Events 1-18: repeated list/set experiments with runtime and wrong-answer feedback.
- Event 25: first public `3/3`; event 26: private `0/3`, revealing an order/type bug the public tests missed.
- Event 34: brief private `100`, but it does not stabilize.
- Events 35-125: long oscillation among private `67`, `33`, and `0` while set-like reasoning keeps returning.
- Event 218: final submission is only `33`.

What it illustrates about student learning:

- Use this replay to teach regression control: keep a working version, isolate the hidden-case hypothesis, and avoid rewriting away a partial success.
- It is also a strong example of how repeated execution can mask weak reasoning about invariants.

How it can inform question design:

- This question benefits from hidden tests because order bugs are hard to expose with only simple public examples.
- It is an excellent talk question precisely because the wrong solutions are often locally plausible.

## Funny Moments To Use On Stage

These are kind laughs, not ridicule. The humor comes from recognizable debugging behavior, overfitting, and Python misunderstandings that many instructors have seen before.

Timestamp note:

- When a prebuilt replay exists, the timestamp below refers to the replay/cast timing.
- Otherwise it refers to observed session time from the canonical timeline plus the event number.

### Python With A JavaScript Accent

- Question: `Check For Greeting Prefix`
- Namespace / problem: `ns_25t2_py22_1 / 15`
- Student ID: `60f6e5f27899406ea16a5470210db8d1`
- Timestamp: Replay `0:00-0:03` (events `1-3`)
- Replay artifacts: [narrative JSON](./replay-ns_25t2_py22_1-15-60f6e5f27899406ea16a5470210db8d1.json), [asciinema replay](./replay-ns_25t2_py22_1-15-60f6e5f27899406ea16a5470210db8d1.rec)
- What happens: The student tries `s.startswith('Hello'|| 'Hi')`, then `s.startswith('Hello' or 'Hi')`, before finally writing the real two-prefix check.
- Why it is funny: It is instantly recognizable to an educator audience as 'Python spoken with JavaScript and English grammar mixed together'. The intent is correct; the operators are from another universe.
- What it reveals: Students often understand the goal before they understand the language-specific operator semantics. Surface fluency can outrun actual Python fluency.
- How to present it hilariously: Read the line out loud and call it 'Python, but with a JavaScript accent.' Let the audience catch the bug before you explain it.

### Negotiating With The Autograder

- Question: `Check For Greeting Prefix`
- Namespace / problem: `ns_25t2_py22_1 / 15`
- Student ID: `60f6e5f27899406ea16a5470210db8d1`
- Timestamp: Replay `~59.7s` (event `81`)
- Replay artifacts: [narrative JSON](./replay-ns_25t2_py22_1-15-60f6e5f27899406ea16a5470210db8d1.json), [asciinema replay](./replay-ns_25t2_py22_1-15-60f6e5f27899406ea16a5470210db8d1.rec)
- What happens: The first all-public-pass version includes a special case like `if s=='Hithere': return False` before a still-brittle prefix rule.
- Why it is funny: It is the purest 'I am not solving the rule; I am bargaining with one visible test case' moment. Most educators have seen exactly this instinct.
- What it reveals: When students are close, they often patch visible counterexamples instead of restating the invariant. Hidden tests matter because they force the conversation back to the rule.
- How to present it hilariously: Describe it as the moment the student enters treaty negotiations with the grader.

### The Triple-Quoted Graveyard

- Question: `Check For Greeting Prefix`
- Namespace / problem: `ns_25t2_py22_1 / 15`
- Student ID: `590240758edf48fa81f701ae4295dc82`
- Timestamp: Observed session time `1:36:49` (event `131`)
- What happens: The final weaker solution stays live, while the better boundary-aware logic is buried inside a triple-quoted comment block.
- Why it is funny: The fix is visibly present and also unusable. It feels like the student held a funeral for the correct solution and left the wrong one in charge.
- What it reveals: Students often use comments as ad hoc version control. That preserves emotional safety, but it also makes regression much more likely.
- How to present it hilariously: Call it 'code necromancy' or 'the triple-quoted graveyard' and show the live wrong code next to the entombed fix.

### The Entire Function Learns One Fruit Sentence

- Question: `Shuffle a Three Word Sentence`
- Namespace / problem: `ns_25t3_py13_1 / 7`
- Student ID: `384851c6834647139983873aea99d419`
- Timestamp: Observed session time `2:09` (event `4`)
- What happens: After a few public reruns, the function still contains `if order==(0, 2, 1): return 'apple orange banana'` and nonetheless reaches public `3/3` before failing private `0/3`.
- Why it is funny: It is hard not to laugh when the whole program seems convinced that all sentence-shuffling in Python is secretly about one fruit salad.
- What it reveals: This is sample overfitting in its purest form: the student has learned the visible example, not the transformation rule.
- How to present it hilariously: Read the hard-coded return line verbatim and say: 'the function has memorized exactly one sentence.'

### Why Use A Tuple When You Can List Every Universe

- Question: `Shuffle a Three Word Sentence`
- Namespace / problem: `ns_25t3_py13_1 / 7`
- Student ID: `107337b2583a4bfebe3e917b315d2684`
- Timestamp: Observed session time `8:21` (event `7`)
- What happens: The student eventually passes by writing all six `if/elif` branches for the six possible three-word permutations.
- Why it is funny: It feels like watching someone defeat abstraction by sheer determination. The program solves the entire permutation universe one branch at a time.
- What it reveals: Students sometimes prefer exhaustive case coverage to a data-driven rule, especially when the state space is visibly tiny.
- How to present it hilariously: Introduce it as 'the moment brute force wins a small, perfectly legal victory over elegance.'

### The Pangram Checker That Forgot C

- Question: `Pangram Check`
- Namespace / problem: `ns_25t2_py21_2 / 18`
- Student ID: `a2316b024ae946b59ebe2f04090321d9`
- Timestamp: Observed session time `0:00` (event `1`)
- What happens: The code checks letters against `'absdefghijklmnopqrstuvwxyz'`, an alphabet string that omits `c`.
- Why it is funny: The function whose whole job is to verify all letters starts by forgetting one. It is funny because the student's internal oracle is itself buggy.
- What it reveals: Under cognitive load, students can make mistakes in the very representation they plan to trust. Even the checking mechanism may need checking.
- How to present it hilariously: Put the alphabet on the slide, pause, and let the audience discover the missing `c` themselves.

### A Pangram Is Apparently 'Lots Of Letters'

- Question: `Pangram Check`
- Namespace / problem: `ns_25t2_py21_2 / 18`
- Student ID: `2ee6740d56614ebbb3e68f6fe2992f28`
- Timestamp: Replay `46.9s-47.2s` (events `61-62`)
- Replay artifacts: [narrative JSON](./replay-ns_25t2_py21_2-18-2ee6740d56614ebbb3e68f6fe2992f28.json), [asciinema replay](./replay-ns_25t2_py21_2-18-2ee6740d56614ebbb3e68f6fe2992f28.rec)
- What happens: After stripping spaces, the student returns `True` when the alphabetic character `count >= 26`.
- Why it is funny: The function has quietly redefined pangram as 'contains a lot of letters'. Educators immediately imagine 26 copies of `a` getting certified as a pangram.
- What it reveals: This is a textbook shortcut heuristic: the student found a feature correlated with the answer, not the actual invariant.
- How to present it hilariously: Ask the room whether `'aaaaaaaaaaaaaaaaaaaaaaaaaa'` now qualifies as great literature.

### The Hidden Boss Battle Is `10D`

- Question: `Card to Value Tuple`
- Namespace / problem: `ns_25t2_py11_1 / 6`
- Student ID: `ad7a05553f034a4a9766e6061a80ed7f`
- Timestamp: Observed session time `0:56-1:02` (events `2-4`)
- What happens: The student settles on `card[1]` and `card[0]`, gets a triumphant public `2/2`, and then immediately drops to private `1/2` because a two-character rank like `10D` breaks the parser.
- Why it is funny: The solution looks finished until the only two-character card in the deck walks on stage like a hidden boss.
- What it reveals: Students often infer the full input shape from the visible samples. One shape-breaking hidden case can expose that assumption instantly.
- How to present it hilariously: Build suspense around the villain card `10D` and present it as the moment the deck fights back.

### Calling `tuple` Like It Needs Two Arguments

- Question: `Card to Value Tuple`
- Namespace / problem: `ns_25t2_py11_1 / 6`
- Student ID: `56036b0cfd0a453da93c959861c50f96`
- Timestamp: Observed session time `0:00-0:14` (events `1-2`)
- What happens: The student computes the right two numbers, then returns `tuple(a,b)`, fixes that to `(a,b)`, and only after that notices the tuple order is still backwards.
- Why it is funny: Everything is right except the punctuation, and then everything is right except left and right.
- What it reveals: Novices can understand the computation yet still be fighting Python constructors and exact output shape at the same time.
- How to present it hilariously: Pause on `tuple(a,b)` and ask the room whether Python feels like receiving two arguments today.

### Solving A Character Problem With `split()`

- Question: `Find Characters Appearing More Than Once`
- Namespace / problem: `ns_25t3_py13_1 / 10`
- Student ID: `4c7d49f2a52e47aea20bf101cc95693f`
- Timestamp: Observed session time `0:00` (event `1`)
- What happens: The first attempt is `t = s.split(); return t` for a question about repeated characters inside one string.
- Why it is funny: It is funny because the student is solving a perfectly reasonable word problem, just not the problem on the exam.
- What it reveals: A lot of early debugging is really representation repair. Students may need to re-decide what the data unit even is before they can solve the task.
- How to present it hilariously: Introduce it as 'the moment the student temporarily changes the syllabus from characters to words.'

### The Grader Becomes An Accomplice

- Question: `Find Characters Appearing More Than Once`
- Namespace / problem: `ns_25t3_py13_1 / 10`
- Student ID: `ee012cee3fa5491d8db37141d2a954fe`
- Timestamp: Replay `17.9s-18.4s` (events `24-26`)
- Replay artifacts: [narrative JSON](./replay-ns_25t3_py13_1-10-ee012cee3fa5491d8db37141d2a954fe.json), [asciinema replay](./replay-ns_25t3_py13_1-10-ee012cee3fa5491d8db37141d2a954fe.rec)
- What happens: The student builds the answer by iterating over `set(s)`, which destroys first-appearance order, and still gets an immediate public all-pass.
- Why it is funny: The audience can see the abstraction is wrong, but the public tests briefly give it a gold star anyway.
- What it reveals: Public tests can accidentally validate the wrong abstraction. Order-sensitive questions need an adversarial public example, not just hidden ones.
- How to present it hilariously: Tell the audience: 'this is the exact moment the grader becomes an accomplice.'

### A Whole Vowel-Reversal Screenplay For Two Samples

- Question: `Reverse Vowel Order in a String`
- Namespace / problem: `ns_25t2_py14_1 / 10`
- Student ID: `8441c3b982914b779cf15f66e9857474`
- Timestamp: Observed session final state `1:12:09` (event `35`, submission)
- What happens: The final code has `if(g=="Hello"): print("HollE")` and then a second branch for the multiline public sample before falling into unfinished loops.
- Why it is funny: The student has not really written a vowel-reversal algorithm. They have written a tiny stage play starring the visible examples.
- What it reveals: When a transformation feels slippery, some students retreat to memorizing exemplar inputs and outputs instead of extracting the rule.
- How to present it hilariously: Show the two sample branches and say the code has learned the script but not the plot.

### The Entire Solution Is `dc\nba`

- Question: `Swap Diagonal Characters in a 2‑Line String`
- Namespace / problem: `ns_25t3_py22 / 5`
- Student ID: `0c796407724441a482dd8912fa4831c6`
- Timestamp: Observed session final state `1:14:01` (event `11`, submission)
- What happens: The whole function returns `"dc\nba"`, the public sample output, no matter what input it receives.
- Why it is funny: It is the most brutally honest overfit in the repo: not even a fake algorithm, just the answer sheet.
- What it reveals: Some students temporarily interpret the public example as the task itself when the transformation rule is not yet internalized.
- How to present it hilariously: Put the full function on screen. The room gets the joke before you finish reading it.

### Routing By The First Character

- Question: `Swap Diagonal Characters in a 2‑Line String`
- Namespace / problem: `ns_25t3_py22 / 5`
- Student ID: `b327a9e2a2cb49c38e502835f4959f10`
- Timestamp: Observed session final state `2:37` (event `13`, submission)
- What happens: The code branches on `s[0:1]` and chooses between `wz\nyx`, `43\n21`, and `dc\nba`.
- Why it is funny: The function behaves like a tiny oracle for three seen inputs and nothing else.
- What it reveals: Visible examples can turn into a lookup table when students do not trust themselves to generalize the positional rule.
- How to present it hilariously: Frame it as a three-entry customer-support menu for the grader.

### The Duplicate Remover Memorizes Six Whole Words

- Question: `Remove Duplicate Characters from String`
- Namespace / problem: `ns_25t3_py11 / 10`
- Student ID: `3d6f6d72d5c546a091659808509c4fce`
- Timestamp: Observed session final state `1:13:48` (event `55`, submission)
- What happens: The final function has exact branches for `banana`, `hello`, `abc`, `python`, `apple`, and `world`.
- Why it is funny: Instead of removing duplicate characters, the program has built a tiny dictionary of favorite English words.
- What it reveals: Sample memorization is not always laziness. It is often a fallback when the student cannot stabilize a general procedure.
- How to present it hilariously: Read the words like flash cards and let the audience notice the algorithm has become a phrasebook.

### The Username Generator Knows Exactly One User

- Question: `Create Username from First Name and User ID`
- Namespace / problem: `ns_25t3_py22 / 6`
- Student ID: `47a311a308e745228f5d3b9cda3048a9`
- Timestamp: Observed session final state `1:14:56` (event `28`, submission)
- What happens: The function ignores its parameters and simply returns `s='ali123'`.
- Why it is funny: The task asks for a username generator; the code replies with one already-generated username and calls it a day.
- What it reveals: When students overfit samples, they may freeze the whole function at the example output rather than even approximate the rule.
- How to present it hilariously: Say: 'this is less a function than a commemorative plaque for Ali.'

### The Pattern Generator Gives Up After Five

- Question: `File Content Zig-Zag Shift`
- Namespace / problem: `ns_25t2_py21_1 / 20`
- Student ID: `242f6a6bda8d40adb0a6324bf648b44e`
- Timestamp: Observed session final state `1:04:10` (event `50`, submission)
- What happens: The program branches on `n == 1` through `n == 5` and prints a handcrafted triangle for each case.
- Why it is funny: It solves pattern generation by refusing to generate patterns once `n` gets ambitious.
- What it reveals: Students will often trade algorithm design for case listing when the visible range of examples feels finite.
- How to present it hilariously: Scroll just far enough to make the audience realize there is a new `elif` for every integer.

### Answering The Wrong Exam With Confidence

- Question: `File Content Zig-Zag Shift`
- Namespace / problem: `ns_25t2_py21_2 / 26`
- Student ID: `149b3b661c744be48fb090042a0fe3b4`
- Timestamp: Observed session final state `1:22:37` (event `71`, submission)
- What happens: The solution opens `filename`, reads lines, and prints a spacing pattern from a different file-I/O zig-zag problem.
- Why it is funny: It is funny because the code is earnest, detailed, and confidently solving a task the grader never asked.
- What it reveals: Question transfer can misfire. Students sometimes recognize a familiar surface form and retrieve the wrong template.
- How to present it hilariously: Call it a beautiful answer to a question that exists on some parallel exam.

## Funny Questions To Cover In Order

This is the funny-first stage sequence rather than the full pedagogical set. It prioritizes questions where the joke lands immediately and then turns into a teaching point.

- Check For Greeting Prefix
  - Student `60f6e5f27899406ea16a5470210db8d1`: `s.startswith('Hello'|| 'Hi')`: Cross-language syntax confusion lands instantly and opens the theme that intent can outrun Python fluency.
  - Student `60f6e5f27899406ea16a5470210db8d1`: `if s=='Hithere': return False`: A perfect visible-test bargaining moment; the audience sees the student patching one sample instead of stating the rule.

- Shuffle a Three Word Sentence
  - Student `384851c6834647139983873aea99d419`: `if order==(0, 2, 1): return 'apple orange banana'`: The purest sample memorization clip in the repo; the whole algorithm learns one fruit sentence.
  - Student `107337b2583a4bfebe3e917b315d2684`: `six if/elif branches, one for each permutation`: Brute force beats abstraction in a way that is both funny and immediately understandable.

- Reverse Vowel Order in a String
  - Student `8441c3b982914b779cf15f66e9857474`: `if(g=="Hello"): print("HollE") ... elif(g=='''Hello, World! ...'''): ...`: The student writes a tiny screenplay for the samples instead of a vowel-reversal algorithm.

- Swap Diagonal Characters in a 2-Line String
  - Student `0c796407724441a482dd8912fa4831c6`: `return "dc\nba"`: The entire function is just the public sample output; the joke is visible in one line.
  - Student `b327a9e2a2cb49c38e502835f4959f10`: `if s[0:1]=='x': 'wz\nyx' elif s[0:1]=='1': '43\n21' else: 'dc\nba'`: A hilariously specific sample router that shows how examples can become a lookup table.

- Remove Duplicate Characters from String
  - Student `3d6f6d72d5c546a091659808509c4fce`: `if s == "banana": return "ban" ...`: The function memorizes example words like flash cards instead of learning character deduplication.
  - Student `d863584c1d954bc181e654049e154321`: `lis = s.split()`: The wrong unit of analysis is obvious and funny: the student turns a character problem into a word problem.

- Card to Value Tuple
  - Student `56036b0cfd0a453da93c959861c50f96`: `return tuple(a,b)`: Everything is right except the punctuation, and then everything is right except the tuple order.
  - Student `ad7a05553f034a4a9766e6061a80ed7f`: `rank = card[0]; suit = card[1]  # defeated by 10D`: A clean hidden-boss moment: the parser looks finished until one two-character rank walks on stage.

- Pangram Check
  - Student `a2316b024ae946b59ebe2f04090321d9`: `letters = "absdefghijklmnopqrstuvwxyz"`: The pangram checker forgets `c`, which is visually funny before you even explain the bug.
  - Student `2ee6740d56614ebbb3e68f6fe2992f28`: `return count >= 26`: It quietly redefines pangram as 'contains lots of letters,' which is a great heuristic-vs-invariant punchline.

- Find Characters Appearing More Than Once
  - Student `4c7d49f2a52e47aea20bf101cc95693f`: `t = s.split(); return t`: Another wrong-unit moment that lands quickly because the student is solving a word problem instead.
  - Student `ee012cee3fa5491d8db37141d2a954fe`: `for c in set(s): ...`: The grader briefly blesses the wrong abstraction, which makes the joke useful for question-design discussion too.

- Create Username from First Name and User ID
  - Student `47a311a308e745228f5d3b9cda3048a9`: `s='ali123'; return(s)`: The function knows exactly one person, which makes sample overfitting instantly legible.

- File Content Zig-Zag Shift
  - Student `242f6a6bda8d40adb0a6324bf648b44e`: `elif n == 1 ... elif n == 5 ...`: Funny as a lookup-table anti-pattern, though it needs a bit more setup than the shorter string examples.
  - Student `149b3b661c744be48fb090042a0fe3b4`: `with open(filename, 'r') as f:`: It is a beautiful 'solved the wrong exam' clip when you want a bigger laugh after the audience trusts the pattern.
